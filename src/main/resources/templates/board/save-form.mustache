{{>layout/header}}



<div class="container p-5">

    <!-- 요청을 하면 localhost:8080/board/save POST로 요청됨
    title=사용자입력값&content=사용자값 -->
    <div class="card">
        <div class="card-header"><b>글쓰기 화면입니다</b></div>
        <div class="card-body">
            <!--enctype="application/x-www-form-urlencoded"-->
            <!--key=value&key=value-->
            <!-- 이렇게 보낼 것이다. title=제목1&content=내용1-->
            <form action="/board/save" method="post" enctype="application/x-www-form-urlencoded">
                <div class="mb-3">
                    <input type="text" class="form-control" placeholder="Enter title" name="subtitle">
                </div>
                <div class="mb-3">
                    <textarea class="form-control" rows="5" name="postContent"></textarea>
                </div>
                <button class="btn btn-primary form-control">글쓰기완료</button>
            </form>
        </div>
    </div>
</div>

{{>layout/footer}}